# -- Markup Configuration --
# Required for theme to function properly

[goldmark]
  [goldmark.renderer]
    unsafe = true  # Allow raw HTML in markdown

[highlight]
  noClasses = false

[tableOfContents]
  startLevel = 2
  endLevel = 4
